/**
 * Re-export wrapper for backward compatibility.
 * Implementation moved to canvas-api-shared.ts for reuse across generators.
 */
export { generateShapeDrawCode as generateShapeCode } from "./canvas-api-shared";
